export type User = { // example
  id: number;
  name: string;
} | undefined;

export type Beatmap = { // example
  id: number;
  artist: string;
  title: string;
  source: string;
  content?: string;
};

// Score for a score
